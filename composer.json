{
	"name": "wp-kit/kernel",
    "description": "A Themosis component that handles routing by acting as a kernel and allows headers to be sent",
    "keywords": ["wp-kit", "themosis", "wordpress", "routing", "headers"],
    "license": "MIT",
	"autoload": {
        "psr-4": {
            "WPKit\\Kernel\\": "src/Kernel"
        }
    }
}
